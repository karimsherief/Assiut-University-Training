var nums = {}
readline();
readline().split` `.forEach((num, index) => {
    if (x <= 10) {
        nums[index] = num
    }
})


for (var key in nums) {
    print(`A[${key}] = ${nums[key]}`)

}